form.col.s12(action='/accounts/login', method='POST')
    .row
        .container.center
            h5 Login
        .input-field.col.s12.l6
            input(type='hidden', name='_csrf', value=csrf)
        .input-field.col.s12
            input#username.validate(type='text', name='username' required)
            label(for='username') username
        .input-field.col.s12
            input#icon_prefix.validate(type='password', name='password' required)
            label(for='icon_prefix') Password
        .col.s12
            button.waves-effect.waves-light.btn
                | LOG IN
            p  Don't have an account? 
                span
                    strong
                        | Click here to 
                        a(href='/accounts/sign-up') Sign Up
